
# Cron script to create daily backup at 02:00
# crontab entry (run 'crontab -e' and add the following line):
# 0 2 * * * /usr/bin/python3 /path/to/cores_fragrancias/scripts/daily_backup.py >> /var/log/cores_backup.log 2>&1
